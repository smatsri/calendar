import { HeaderContainer } from "./styles";

type HeaderProps = {}

const Header = ({ }: HeaderProps) => {
  return (
    <HeaderContainer>
      
    </HeaderContainer>
  );
}

export default Header;
